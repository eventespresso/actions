{
	"author": "Event Espresso",
	"name": "@eventespresso-actions/visual-regression-tests",
	"private": true,
	"version": "1.0.0",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsc",
		"format": "prettier --write **/*.ts",
		"format-check": "prettier --check **/*.ts",
		"lint": "eslint src/**/*.ts",
		"all": "npm run build && npm run format && npm run lint"
	},
	"license": "GPL-3.0"
}
